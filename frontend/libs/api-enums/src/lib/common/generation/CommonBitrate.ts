// NOTE: This is defined in Rust (as the source of truth) and duplicated in the frontend.
// In the future, code generation should keep the two sides in sync.

export enum CommonBitrate {
  Normal = "normal",
  High = "high",
}
